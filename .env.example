# Public base URL for the backend API that the frontend calls (Client-side)
# If not set, frontend falls back to relative '/api' (e.g. when using a local proxy/rewrites)
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:5001/api
